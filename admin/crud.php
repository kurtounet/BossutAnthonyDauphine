<?php
require_once("../utils/databaseManager.php");


/*
if (isset($_SESSION["username"]) === false) {
    header("Location: https://localhost/pokedex-gaetan/login.php");
}else{
    
}
if ($_GET['action'] === 'delete') {
   // deletePokemon($pdo, $_GET['id']);
} elseif ($_GET['action'] === 'edit') {
   // editPokemon($pdo, $_GET['id']);
} elseif ($_GET['action'] === 'create') {
   // createPokemon($pdo);
}



//$pokemons = findPokemonById(PDO $pdo, int $id): ar

var_dump($_GET);

//$pokemons = findAllPokemons($pdo);
/*
$passHash = password_hash("admin", PASSWORD_DEFAULT);
echo ($passHash);
var_dump(password_verify("admin", $passHash));
*/
